node_modules/
dist/
target/
.env
.env.*
*.log

# Local runtime / editor scratch — never part of the repo
.claude/
server/seed-demo.ts

# Internal — kept locally, never published: the design spec, the handoff
# notes, the implementation plans, the pitch material, and the local
# file->artifact mapping for whatever deployment this checkout pushes to.
artef-spec.md
docs/
.artef.json
